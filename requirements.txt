# Pinned, known-good dependency set (macOS / Linux, Python 3.10+).
# Runtime + test tooling. For an exact reproducible install:  pip install -r requirements.txt
# (pyproject.toml is the flexible source of truth: pip install -e ".[dev]")
#
# The native window uses pywebview, kept separate because it pulls heavy macOS bindings:
#   pip install "pywebview==6.2.1"

# --- runtime ---
fastapi==0.141.1
# starlette is FastAPI's ASGI core (transitive). Pinned explicitly because 0.52.x carried 7 CVEs
# (Host-header URL reconstruction, multipart limits) fixed in 1.x — and the security floor should be
# reviewable here, not buried in whatever FastAPI happens to resolve. FastAPI dropped its
# `starlette<1.0` cap, so 1.x is allowed; the whole suite passes on it.
starlette==1.6.0
uvicorn[standard]==0.39.0
jinja2==3.1.6
python-multipart==0.0.32
keyring==25.7.0

# --- tests ---
pytest==9.0.3
pytest-asyncio==1.4.0
httpx==0.28.1
