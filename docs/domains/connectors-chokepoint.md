# Domain: Connectors — the write chokepoint

**One line:** The `GAMConnector` translates high-level operations into `GAMCommands` argv, and every mutation the app makes funnels through its single `_run_write` helper, which serializes, redacts, runs, and audits the call.

**Owns invariant(s):** #2 (every mutation goes through the chokepoint; no second write path; `audit_argv` redaction). Leans on #1 (argv-only builders) and #4 (serialization protects the ephemeral GAMCFGDIR).
**Enforcement home:** `tests/test_gam_connector.py`, `tests/test_audit.py` (redaction), `tests/test_onboarding.py` (redaction by value on a failed `create_user`), `tests/test_lifecycle.py` (tolerate/serialize), `tests/test_runner.py` (serialize + token write-back). The second-write-path tripwires are in `tests/test_command_contract.py`: `test_audit_record_only_in_run_write_or_allowlist` (every `audit.record(`) `test_every_run_authenticated_call_is_the_chokepoint_or_a_read` (every `run_authenticated(` in `gamgui/`) and `test_gam_is_spawned_only_through_run_authenticated_or_version` (no side door to the runner's `_exec`).

## Files
- `gamgui/core/connectors/gam_connector.py` — the Google Workspace connector; all read/mutation methods + the `_run_write` chokepoint and `apply`/`plan_suspend`.
- `gamgui/core/connectors/base.py` — `Connector` base, `ChangePreview`/`ChangeResult`, `RiskLevel`, `ConnectorID`, `Capability`, `LifecycleAction`.
- `gamgui/core/connectors/abapit_connector.py` — optional ABM/Mosyle wrapper (lazy import; separate system, not GAM).
- `gamgui/core/connectors/person.py` — `Person` / `ConnectorAccount` identity DTOs.
- `gamgui/core/guard.py` — `evaluate()` → `GuardDecision`; the confirmation-policy gate (see flow note).
- `gamgui/core/audit.py` — `AuditLog.record`, `redact_argv`, rolled generations.
- `gamgui/core/gam/runner.py` — `GAMRunner.run_authenticated(..., serialize=True)`, the only subprocess.

## How it works
A mutation is: connector method (e.g. `set_signature`, `add_group_member`, `delete_user`) builds argv via a `GAMCommands` static method, then calls `await self._run_write(action, target, argv, RiskLevel, ...)`. `_run_write` wraps it in a `ChangePreview`, runs `self.runner.run_authenticated(self.domain, argv, serialize=True)`, and on both success and failure calls `self.audit.record(...)`, returning a `ChangeResult` — on a failure, `detail` is GAM's raw error (redacted) and `remediation` says what to do in words (`GAMError.remediation`, or a generic line when the call failed before GAM ran), and `kind` is the `GAMErrorKind` (None on success or a non-GAM error) — what a bulk loop reads to stop on an account-wide failure (`gam.errors.ACCOUNT_WIDE_KINDS`, `core/bulk.py` `stop_reason`). A route should show `remediation` first and `detail` behind a disclosure (`_action_result.html`'s `details`); the signature flows, delegation and every other user-detail write do (`write_failed` in `web/routes/_common.py`: sign-out, groups, title/department, calendar access, auto-reply, suspend, delete — plan U9, `test_a_failed_user_write_says_why_with_gams_error_expandable`); so do the Calendars screen's share/unshare/delete and the Groups board's add/remove (`7b3fe49`). Planned changes (`plan_suspend` → `apply`) route through the same `_run_write`. The guard is NOT called inside `_run_write` — it runs earlier, in the web routes: the preview route renders `guard.evaluate()`'s decision, and the apply route calls `guard.enforce(previews, form)` to refuse a POST that lacks the confirmation before any write (see `docs/domains/guard-audit.md`); `_run_write` is the audit/serialization point, the guard is the confirmation policy and its server-side check.

## Invariants & the failure history
- **Single write path (#2).** All writes go through `_run_write`; `apply()` delegates to it, and the Builder's `run` route calls `conn.apply([preview])` for mutations. Every other `run_authenticated` call is a read or the one hand-audited exception below, and `test_every_run_authenticated_call_is_the_chokepoint_or_a_read` enforces it: a call site outside `_run_write` / `AUDITED_OUTSIDE_RUN_WRITE` / `READ_BY_CONTRACT` must trace its argv to a builder classified as a read in `tests/test_mock_gam.py` and must not take the write lock. Two writes once slipped past the `audit.record` tripwire simply by never being audited: the Builder's `todrive` export (it creates a Sheet, in another user's Drive via `tduser`) and `reset_password`'s follow-up sign-out (failure-log 2026-09-23). Both now run through `_run_write` — `export_to_sheet`, and the sign-out as offboarding's own `revoke_access` step (`reset_password` runs only the reset since its swallowed failure, failure-log 2026-09-23).
- **The Builder's reads are reads by contract.** `catalog_read(cmd, argv, target)` runs a catalog command's argv, which no static check can trace to one builder, so it refuses any `cmd` the catalog doesn't mark `READ_ONLY` (`export_to_sheet` too). For a `cmd.sensitive` read it also records `sensitive_read` — never the output (see `docs/domains/guard-audit.md`); the Builder runs a sensitive sequence step through it too. `audit_sensitive_csv` records the Builder's CSV download of such a result (`sensitive_csv_export`, no GAM call). `ChangeResult.output` carries a write's stdout (the Sheet URL an export prints), secrets masked.
- **Redaction is by value first, positional second.** `_run_write(secrets=[pw])` masks every occurrence of the secret in the shown argv, `str(exc)` (GAM echoes the command line on a usage error) → the audit's `extra.error` and `ChangeResult.detail`, and every audited field (`record(secrets=)`); `create_user` passes its temp password. By value because the positional masks shift: a hire surnamed `Password` made `lastname Password password <pw>` mask the keyword and leak `<pw>` into the log and the error page (failure-log 2026-09-23). The second layer: `audit_argv=` (create_user audits a `"********"` argv; the real one still runs), `audit.redact_argv` (value after `password, notifypassword, signature, recoveryemail, recoveryphone, alternateemail`) and `errors._scrub_stderr`. The docstring: the shown argv is "never the raw secret". `reset_password` sends `password random` — GAM picks the value, so the connector has no secret to pass.
- **An interrupted write is audited** (2026-09-23, review F3). Quitting cancels an in-flight job
  (`server._lifespan`); `CancelledError` is a `BaseException`, so `_run_write`'s `except Exception`
  never saw it and the write left no record — not even the hour-long calendar sweep, which may have
  changed part of the domain. `_run_write` now records it `ok: false` with `extra.error` =
  `INTERRUPTED` ("interrupted … it may have done part of its work"), after the runner has stopped `gam`
  and wiped its dir, then re-raises. Proven through the real quit path (a `TestClient` shutdown)
  by `test_lifecycle.py::test_quitting_mid_offboard_stops_the_sweep_and_audits_it`.
  `create_onboarding_runbook` (below) still catches only `Exception`.
- **`tolerate_kinds` for best-effort sweeps.** `remove_from_all_calendars` passes `SWEEP_TOLERATED` = `(NOT_FOUND, SERVICE_NOT_ENABLED, OWN_ACL)` so per-user "never shared", a user without Calendar ("Calendar Service/App not enabled") and the departing user's own-primary `cannotChangeOwnAcl` (GAM exit 50) count as success and are audited `ok, tolerated=True`. A generic 403 (`PERMISSION_DENIED`) is not tolerated: it once was, wholesale (failure-log 2026-09-23). Regression fixed in b26da7e / tests `test_offboard_calendar_sweep_tolerates_own_acl`, `test_remove_from_all_calendars_does_not_tolerate_a_permission_refusal`.
- **Serialization (#4).** `serialize=True` takes the runner's `_write_lock` so two writes can't race the same ephemeral GAMCFGDIR; it also gives the oauth-token write-back a stable dir (`test_oauth_token_write_back_through_a_real_run`).
- **The one serialized non-`_run_write` write path.** `create_onboarding_runbook` (tasklist + per-task loop) runs via a direct `runner.run_authenticated(..., serialize=True)` and audits itself (`self.audit.record("onboard_runbook", ...)`); it is the only entry in `AUDITED_OUTSIDE_RUN_WRITE`. `reset_password`'s best-effort follow-up sign-out used to be a second, un-audited one; it is gone — offboarding signs out in its own step (`revoke_access`, `_run_write`), whose failure is a `✗`. Don't treat the runbook as a licence to add more.

## Gotchas / mock-lies traps
- `RiskLevel` on each `_run_write` call must match the real GAM verb's blast radius, and the catalog's declared risk must match it (see `core/catalog/catalog.py`). `delete_calendar` uses GAM's `remove calendars` (= Calendars.delete, permanent) not `delete calendars` (unsubscribe) — verified against GAM7 source, comment in `delete_calendar`.
- `formatjson` traps (MEMORY: gamgui-gam-formatjson): `show signature`/`show vacation` are parsed as *text* here (`_parse_signature`, `Vacation.from_show_text`), not JSON — do not "modernize" them to formatjson. The mock cannot catch a formatjson rejection; check `gamgui/resources/gam7/GamCommands.txt`.
- `mock_gam.sh` returned members for any address once, making a user look like a group — a permissive mock converts a live break into a green test. Anything mutating (create/delete/transfer/ACL) is only *proven* against a real throwaway tenant; passing tests do not prove a write works.

## Testing / live-verification status
`.venv/bin/python -m pytest -q` runs fully offline: the `connector` fixture (`tests/conftest.py`) wires a `GAMRunner` at `mock_gam.sh` (its `gam_binary` argument) + an in-memory vault, audit to a tmp file. Covered: read parsing, `set_signature` redaction, `plan_suspend` risk, `apply`, vacation set/clear, redaction masks (`test_audit.py`), tolerate-own-acl and the merged-transfer rule (Drive+Calendar go out as one `create datatransfer` to avoid a same-user 409 — `test_lifecycle.py`), serialize + token write-back (`test_runner.py`). Which writes have run against a real tenant is the README's live-verification table (every write the app can perform, one row each). Not yet among the ones here: `delete_user`, `delete_event`, suspend, the single-call `transfer_data` (the old two-call form ran live), the calendar-ACL sweep, `create_user`, the Tasks checklist and welcome mail, and the Sheet export — verify on a throwaway user/event/calendar per CLAUDE.md rules of engagement.

## To do common tasks here
- **Add a new mutation:** add the argv builder in `core/gam/commands.py` (argv-only, #1) → add an `async def` on `GAMConnector` that calls `self._run_write(action, target, argv, RiskLevel.X, ...)` → pick the honest `RiskLevel` → if it carries a secret, pass the value as `secrets=[...]` (plus a redacted `audit_argv`) and confirm the sensitive key is in `_SENSITIVE_KEYS` → wire the route (usually via `conn.apply([preview])` after `guard.enforce`, and classify it in `tests/test_write_routes_guarded.py`). Add a connector test + make `mock_gam.sh` fail the way real GAM fails.
- **Make a bulk sweep tolerate expected per-entity errors:** map the GAM exit to a `GAMErrorKind` in `core/gam/errors.py`, then pass `tolerate_kinds=(...)` to `_run_write`.
- **Redact a new secret field:** pass the value as `_run_write(..., secrets=[value])`; also add the gam key to `_SENSITIVE_KEYS` in `audit.py` with a `test_audit.py` case (and an explicit `audit_argv` for a positional secret with no key token before it).
