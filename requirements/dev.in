# What CI runs the suite, ruff and mypy with. Locked into dev.txt by `make lock`. Keep these in step
# with pyproject.toml's dependencies and dev extra (tests/test_locks.py fails if they drift).
fastapi>=0.141
starlette>=1.3.1
uvicorn>=0.29
jinja2>=3.1
python-multipart>=0.0.31
keyring>=24
pytest>=9.0.3
pytest-asyncio>=0.23
pytest-timeout>=2.3
pytest-cov>=5
httpx>=0.27
ruff==0.16.8
mypy==2.3.1
uv==0.12.18
